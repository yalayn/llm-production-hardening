<!--
USER STORY TEMPLATE.
Copy to Spec/stories/SPRINT-<YYYY-MM>-<sprintId>.md (one per sprint) and fill in.
Stories are an ORIGIN of the idea: agnostic and OPTIONAL. Their only output is to
feed a spec. Specs cite them by id.
-->

# User stories — SPRINT-<YYYY-MM>-<sprintId> <<short sprint goal>>

| Field | Value |
|---|---|
| **Sprint** | `SPRINT-<YYYY-MM>-<sprintId>` <<prefix + starting month + descriptive sprintId, no sequential number>> |
| **sprintId** | `<sprint-id>` <<lowercase kebab-case, descriptive, unique within the month>> |
| **Goal** | <<what the sprint is after, one sentence>> |
| **Period** | <<YYYY-MM-DD → YYYY-MM-DD, optional>> |

---

## HU-001 · <<short story title>>

**As** `<role>` **I want** `<capability>` **so that** `<benefit>`.

- **Priority:** <<high · medium · low>>

**Acceptance criteria** <<verifiable; they feed the spec's criteria>>

- **Given** <<...>> **when** <<...>> **then** <<...>>
